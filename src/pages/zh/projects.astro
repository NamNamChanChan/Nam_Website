---
// Thin /zh wrapper: the page reads Astro.currentLocale, so rendering the
// same component under /zh yields the Chinese version.
import ProjectsPage from "../projects.astro";
---

<ProjectsPage />
